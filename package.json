{
  "name": "library-breaking-change-examples",
  "version": "0.1.0",
  "description": "examples to show library breaking changes",
  "scripts": {
    "bootstrap": "git clone --single-branch --branch contract-update https://github.com/trufflesuite/truffle.git && cd currentLib && npm run bootstrap && cd ../newLib && npm run bootstrap",
    "test": "cd currentLib && npm run test && cd ../newLib && npm run test"
  },
  "author": "Cruz Molina",
  "license": "MIT"
}
